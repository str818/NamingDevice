#### 1、放置文件

将文件夹内的文件放到路径 ```D:\Files\exe``` 下。

#### 2、运行

双击「点名器.exe」运行程序

#### 3、更换闪屏图片

替换文件夹内的  ```pic.jpg``` 图片，注意，命名还是  ```pic.jpg``` ，严格注意命名格式与图片类型，否则不能正确加载。

#### 4、更换点名名单

修改文件夹内的 ```NameList.txt``` 文件，文件内每一行的格式为「序号+空格+姓名+空格+概率等级」（格式不正确会导致缺失成员），注意不要修改此文件名称，概率等级分为 1、2、3 三种，请不要填写这三种之外的数字。